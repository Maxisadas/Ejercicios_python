

def median(array1,array2):
    joinArray = array1 + array2
    n = len(joinArray)
    suma = 0
    for elemento in joinArray:
        suma += 0 + float(elemento)
    return 'La media aritmetica es: '+ str(suma/n)

